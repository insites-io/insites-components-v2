# v2.10.6
- [TW#18520208](https://pm.cbo.me/#/tasks/18520208) Cursor changed to `not-allowed` on `disabled` state

## Changes from V1
- Methods are now asynchronous
- Update `onCheckInsCheckbox` to `insCheck` to avoid DOM event conflict
- Update `valueChange` to `insValueChange` to avoid DOM event conflict