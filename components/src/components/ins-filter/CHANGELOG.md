# v2.0.0

## Changes from V1
- Methods are now asynchronous
- Update `onDateFilterApply` to `insDateFilter` to avoid DOM event conflict
- Update `onFilterApply` to `insFilter` to avoid DOM event conflict
- Removed jQueryUI plugin datepicker widget dependency and used flatpickr instead