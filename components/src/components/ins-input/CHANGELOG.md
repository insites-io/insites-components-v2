# v2.1.0
- Implement new props from v1 `unit-left` and `unit-right`

## Changes from V1
- Update `oninput` to `insInput` to avoid DOM event conflict
- Update `onblur` to `insBlur` to avoid DOM event conflict
- Update `onIconClick` to `insIconClick` to avoid DOM event conflict
- Update `valueChange` to `insValueChange` to avoid DOM event conflict