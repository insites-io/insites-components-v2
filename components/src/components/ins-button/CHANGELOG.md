# v2.0.0

## Changes from V1
- Updated event `onClickInsButton` to `clickInsButton` to describe the event better
- Updated event `onClickInsButtonOption` to `clickInsButtonOption` to describe the event better