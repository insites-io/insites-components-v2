# v2.2.0
- Fixed styles

## Changes from V1
- Updated event `onClickInsButton` to `insClick` to describe the event better
- Updated event `onClickInsButtonOption` to `insClickOption` to describe the event better