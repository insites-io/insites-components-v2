# v2.10.14
- Fixed editor setting default value in code editor mode

# v2.10.5
- Auto-disable rich editor when content has a liquid code

# v2.1.0
- Fixed editor adding contenteditable attribute

## Changes from V1
- Methods are now asynchronous
- Update events with `on` to `ins`
- Update vent `valueChange` to `insValueChange`
