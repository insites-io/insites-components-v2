# v2.0.0

## Changes from V1
- Methods are now asynchronous
- Updated `onDragStart` to `insDragStart` to avoid DOM event conflict
- Updated `onDragEnd` to `insDragEnd` to avoid DOM event conflict
- Updated `onChoose` to `insChoose` to avoid DOM event conflict
- Updated `onUpdate` to `insUpdate` to avoid DOM event conflict
- Updated `onMove` to `insMove` to avoid DOM event conflict
- Updated `onAdd` to `insAdd` to avoid DOM event conflict
- Updated `onRemove` to `insRemove` to avoid DOM event conflict
- Updated `onClone` to `insClone` to avoid DOM event conflict
- Updated `onPositionChanged` to `insPositionChanged` to avoid DOM event conflict
- Updated `onDrop` to `insDrop` to avoid DOM event conflict